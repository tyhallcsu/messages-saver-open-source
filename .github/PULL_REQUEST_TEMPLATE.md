# Pull request

## What does this change?

<!-- One paragraph. What problem is this solving and how? -->

## Screenshots / recordings

<!-- Required for any UI change. Before / after side-by-side if possible. -->

## Checklist

- [ ] I read `CONTRIBUTING.md` and `CLAUDE.md`.
- [ ] No new runtime dependencies (no npm, no bundler, no CDN scripts).
- [ ] No new network calls (`fetch`, `XHR`, `WebSocket`, beacons, etc.).
- [ ] No new manifest permissions (or, if needed, justified below).
- [ ] Docs updated if user-facing behavior changed.
- [ ] `sample-data/` regenerated if the export schema changed.
- [ ] I smoke-tested against a real conversation locally.

## Permission changes (if any)

<!-- Delete if not applicable. Otherwise list the new permissions and
     why they are needed. -->

## Breaking changes (if any)

<!-- Delete if not applicable. Otherwise describe what users / existing
     exports need to do to stay compatible, and bump the JSON schema
     version string. -->
